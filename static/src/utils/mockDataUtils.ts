/**
 * Historical demo fixture module.
 *
 * Production audit code must never fabricate findings. These exports remain
 * only to avoid breaking old imports while intentionally returning no data.
 * Use POST /api/audit for real evidence.
 */
export const generateRealisticScripts = () => [];
export const generateRealisticEvents = () => [];
